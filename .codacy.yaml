exclude_paths:
  - ".github/ISSUE_TEMPLATE/*.yml"
  # Codacy flags the pinned SonarSource action commit as a SonarQube API key.
  - ".github/workflows/sonarqube.yml"

engines:
  prospector:
    exclude_paths:
      - "tests/unit/test_bridge_unit.py"
      - "tests/unit/test_media_backfill_unit.py"
